<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Biometrik: wajib ada keduanya untuk Android 9+ dan Android 13+ -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Kamera & galeri untuk upload peta SPPT -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage Android <= 12 (API 32) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Storage Android 13+ (API 33+): gambar -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Storage Android 13+ (API 33+): dokumen & file lain (CSV, XLSX, dll)
         Dibutuhkan oleh file_picker untuk akses file non-media -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!-- Provider untuk file_picker agar bisa akses file via SAF (Storage Access Framework) -->
    <application
        android:label="Gunpuy Ceria"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
